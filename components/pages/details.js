export default function Details({title, content}) {
 
    return(
  
        <section id="overview">

            <h1> {title} </h1>
            <p class="caption"> {content} </p>

        </section>
       
    )
}